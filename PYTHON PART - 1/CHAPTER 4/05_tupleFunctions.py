# tup = (1,2,3,5,1,7)
tup = (1,2,3,5,1,7,"harsha")

print(tup)

num = tup.count(1)# returns the number of times the 1 is there in tuple
print(num)

index = tup.index("harsha")#gives the index of the entered number in index(*)
print(index)

#we can even use the combination in tuples